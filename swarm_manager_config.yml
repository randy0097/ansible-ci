---
- name: git swarm manager info
  hosts: "{{groups['swarm_manager'][0]}}"
  tasks:
  - name: docker swarm join-token manager      
    command: docker swarm join-token manager|grep docker
    register: add_manager_cmd

- name:  config swarm
  hosts: "{{groups['swarm_manager'][1:]}}"
  roles:
  - role: swarm_manager_config
