---
- name:  deploy tomcat permission
  hosts: "{{groups['swarm_manager'][0]}}"
  vars:
    tomcat_name: permission
    server_xml: server.xml.ssl
    ssl_key: iscas.pfx
  roles:
  - role: tomcat_deploy  

# - name:  deploy tomcat cas
#   hosts: "{{groups['swarm_manager'][0]}}"
#   vars:
#     tomcat_name: cas
#   roles:
#   - role: tomcat_deploy
