---
- name: config GlusterFS
  hosts: docker_servers
  tasks:
  - debug:
    msg: "{{groups['docker_servers'][0]}}"
  - name: configure the trusted pool
    command: gluster peer probe "{{groups['docker_servers'][1]}}"
    when: inventory_hostname == groups['docker_servers'][0]

