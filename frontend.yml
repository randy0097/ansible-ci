---
- name: frontend_compile
  hosts: localhost
  roles:
  - frontend_compile

- name: frontend_deploy
  hosts: "{{env}}"
  roles:
  - { role: frontend_deploy, when: operation == 'deploy' }
