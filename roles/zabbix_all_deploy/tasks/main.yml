- name: create remote temp dir
  file: 
    path: "{{remote_temp}}"
    state: directory 

- name: copy stack.yml to remote
  template: 
    src: zabbix_all_stack.yml 
    dest: "{{remote_temp}}/zabbix_all_stack.yml"

- name: deploy stack
  command: docker stack deploy --compose-file=zabbix_all_stack.yml zabbix_all
  args: 
    chdir: "{{remote_temp}}"


    